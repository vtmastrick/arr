---
services:
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr_uhd
    environment:
      - PUID=2051
      - PGID=8888
      - TZ=America/New_York
    volumes:
      - ./config/:/config
      - /mnt/radarr_uhd/movies:/data/movies #optional
      - /mnt/radarr_uhd/usenet/completed:/data/usenet/downloads/completed #optional
      - /mnt/radarr_uhd/torrents:/data/torrents/downloads #optional
    ports:
      - 7879:7878
    restart: unless-stopped
    networks:
      - arr_network
      - nginx-bridge

networks:
  arr_network:
    external: true
  nginx-bridge:
    external: true

